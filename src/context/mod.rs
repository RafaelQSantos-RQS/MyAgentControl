//! Context system (Phase 1, MAC-CTX): HTML-comment frontmatter parsing,
//! MVI validation, local-first resolution, navigation cross-refs.
//!
//! Task phase1-context-01 ships the [`frontmatter`] parser (CTX-1).

pub mod frontmatter;
